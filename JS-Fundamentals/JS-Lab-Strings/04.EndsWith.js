    function endsWith([text, comparor]){
        let word = text;
        substring = comparor;
        console.log(word.indexOf(substring) !== -1)
    }

endsWith(['The new iPhone has no headphones jack','o headphones jack']);
